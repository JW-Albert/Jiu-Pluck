# Jiu-Pluck Backend 環境變數設定範例
# 複製此檔案為 .env 並填入實際值
# cp .env.example .env

# ==========================================
# 應用程式設定
# ==========================================
APP_ENV=dev
APP_SECRET_KEY=change_me_to_a_secure_random_string_at_least_32_characters_long
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# ==========================================
# 資料庫設定
# ==========================================
DATABASE_URL=sqlite+aiosqlite:///./app.db
# 如需使用 PostgreSQL，可改為：
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/jiu_pluck

# ==========================================
# Email 設定 (SMTP)
# ==========================================
# 開發環境下，Email 驗證碼會直接印在 console，可留空
# 生產環境請務必設定以下變數
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_user
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=your_app@domain.com
SMTP_USE_TLS=true

# ==========================================
# Google Calendar OAuth 設定
# ==========================================
# 設定步驟：
# 1. 前往 https://console.cloud.google.com/
# 2. 建立新專案或選擇現有專案
# 3. 啟用 Google Calendar API
# 4. 建立 OAuth 2.0 憑證 (Web application)
# 5. 設定授權重新導向 URI: http://localhost:8000/api/calendar/google/callback
#    生產環境請改為實際域名
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8000/api/calendar/google/callback
GOOGLE_CALENDAR_SCOPES=https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/calendar.readonly

# ==========================================
# Apple Calendar 設定
# ==========================================
# Apple Calendar 使用 CalDAV，每個使用者需要提供：
# - Apple ID Email
# - App-specific Password (從 https://appleid.apple.com 產生)
# 這些資訊會加密儲存在資料庫中，不需要在此設定全域變數

# ==========================================
# 安全提示
# ==========================================
# 1. APP_SECRET_KEY 必須是隨機且足夠長的字符串（建議至少 32 字元）
#    可使用以下命令產生：python -c "import secrets; print(secrets.token_urlsafe(32))"
# 2. 生產環境請勿將 .env 檔案提交到版本控制系統
# 3. 定期更換敏感資訊（如密碼、API keys）
